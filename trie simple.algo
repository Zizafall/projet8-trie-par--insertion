ALGORITHM trie carte par insertion (tabb)
VAR
    // declaration de variable 
    i;j;arr tab : INTEGER
    BEGIN
    FUNCTION trie insertion (tableau T[i:-1])
    VAR
        FOR i FROM 0 TO - 1 TEP step  DO
        elemt = T[i]
        J = i 
        IF (j > 0 & T[j-1> elemt]) THEN
            j--
        END_IF
        T[j]= elemt
        
        END_FOR
    
    END
    END
    